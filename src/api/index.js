import {get,post} from './http'

export const getZxal = (params) => {
  let url = '';
  return get(url, params)
}
export const getZxal = (params) => {
  let url = '';
  return get(url, params)
}







